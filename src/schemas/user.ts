export type UserSearchResult = {
    userId: string;  // Unique identifier for each user
    username: string;
    email: string;
  };
  
  export type UserProfile = {
    userId: string;  // Unique identifier for each user
    username: string;
    email: string;
  };
  